# Arquivos originais dos certificados: .png, .pdf, .lnk e ate sem extensao
# (o da FIAP veio assim). Ficam na maquina local como backup; o site serve
# apenas as versoes otimizadas em .webp (cert-*.webp), essas sim versionadas.
Certificado*
cert-*-intermediario.png

# Arquivos de sistema do Windows/macOS
Thumbs.db
desktop.ini
.DS_Store

# Editores
.vscode/
.idea/
*.code-workspace

# Dependencias e build (caso o projeto cresca)
node_modules/
dist/
.env
.env.local
